[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "findmyway"
version = "0.0.1"
description = "My findmyway package"
authors = [{name = "Your Name", email = "your@email.com"}]
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["src"]
